{
  "devDependencies": {
    "typescript": "^4.5.2"
  },
  "dependencies": {
    "axios": "^0.24.0"
  },
  "scripts": {
    "start": "npm run start:db && npm run start:parcel",
    "start:db": "json-server -w db.json",
    "start:parcel": "parcel index.html"
  }
}
